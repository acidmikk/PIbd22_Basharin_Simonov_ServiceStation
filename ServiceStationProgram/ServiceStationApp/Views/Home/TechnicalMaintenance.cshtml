@using ServiceStationContracts.ViewModels

@model List<TechnicalMaintenanceViewModel>
@{
    ViewData["Title"] = "TechnicalMaintenances";
}

<div class="text-center">
    <h1 class="display-4">ТО</h1>
</div>

<div class="text-center">
    @{
        if (Model == null)
        {
            <h3 class="display-4">Авторизируйтесь</h3>
            return;
        }
        <p>
            <a class="btn btn-success" asp-action="TechnicalMaintenanceCreate">Добавить ТО</a>
        </p>
        <table id="listingTable" class="table">
            <thead>
                <tr>
                    <th>
                        Номер
                    </th>
                    <th>
                        Наименование
                    </th>
                    <th>
                        Описание
                    </th>
                    <th>
                        Изменение ТО
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Id)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Discription)
                        </td>
                        <td>
                            <a class="btn btn-warning button-fixed button-sm" asp-action="TechnicalMaintenanceUpdate" 
                            asp-route-technicalMaintenanceId="@item.Id" asp-route-name="@item.Name" 
                            asp-route-discriprion="@item.Discription">Изменить</a>

                            <a class="btn btn-danger button-fixed button-sm" asp-action="TechnicalMaintenanceDelete" 
                            asp-route-technicalMaintenanceId="@item.Id">Удалить</a>
                        </td>
                    </tr>
                }
            </tbody>              
        </table>
    }
</div>